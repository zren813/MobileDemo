<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobiledemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileDemo">
        <activity android:name=".ThemePage"></activity>
        <activity android:name=".FontPage" />
        <activity android:name=".DetailedSettingPage" />
        <activity android:name=".CourseListPage" />
        <activity android:name=".SettingPage" />
        <activity android:name=".InfoPage" />
        <activity android:name=".SearchClassPage" />
        <activity
            android:name=".DisplaySearchResult"
            android:exported="true" />
        <activity android:name=".ProfilePage" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>